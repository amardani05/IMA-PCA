"""PCA pipeline diagnostic suite.

Run via ``python -m diagnostics.run_all`` to audit feature coverage / variance,
cluster quality, temporal + subsample stability, individual-stock sanity, and
feature-ablation. Outputs land in ``output/diagnostics/``.
"""
